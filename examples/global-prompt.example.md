# 全局提示词示例

把这个文件复制到 `~/.dsh/global-prompt.md`（或直接在 Web 面板的「全局提示词」里粘贴），
每一轮对话都会带上它。

写几条真正长期有效的硬规则就好——太长的清单会稀释注意力：

- 始终用中文回答；代码、命令、报错原文保持原样。
- 改代码前先读文件，不要凭猜测改；不确定就读源码验证，不要编造 API。
- 改动尽量小而聚焦，顺带修掉的东西要单独说明。
- 完成后给出可复现的验证方式（跑什么命令、期望看到什么）。
